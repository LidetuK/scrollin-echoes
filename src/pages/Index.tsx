import { TestimonialSection } from "@/components/TestimonialSection";

const Index = () => {
  return (
    <div className="w-full min-w-full min-h-screen" style={{ backgroundColor: "#01071A" }}>
      <TestimonialSection />
    </div>
  );
};

export default Index;